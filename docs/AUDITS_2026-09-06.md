# The 2026-09-06 audits — index, headlines, what shipped, what is open

Sixteen read-only audits were dispatched against the store and the code on
2026-09-06 while the observed verification cycle ran. Ten completed and their
full reports are under `docs/audits/2026-09-06/`. Four produced nothing (rate
limit): **Watch Desk trigger quality**, **scheduler / dead timers**, **error
taxonomy**, **book reconciliation** — still open, not done. Two were reviews
of the stalls branch itself (see `STALLS_AND_SILENT_COSTS_2026-09-06.md`).

Every number below is from the report it links; none is a guess.

| audit | headline | shipped |
|---|---|---|
| [outcome_horizon](audits/2026-09-06/outcome_horizon.md) | The "7-day outcome" has a **median actual horizon of 43 days**, 1.37 % within 7 days, 71.7 % beyond 30. The exit fix (1bfd700f, 09-05) has produced **zero rows**. Recomputing 2,241 rows at entry+7d changes **38 % of grades** and flips 27.5 % of signs; win rate 0.609→0.579. On the live 30-day cohort hold accuracy is **0.633 stored vs 0.443 honest**, crossing the <0.50 issue line. `challenger.py:139` still reads latest close with no vendor pin (mean vendor disagreement 20 %). The drifted numbers are fed back to the desks as calibration instruction (`base_agent.py:468`). | nothing — **do not use decision-quality metrics for optimisation until this is remediated** |
| [partial_cost](audits/2026-09-06/partial_cost.md) | The A/B/C decision targets 6.7 % of the problem. **193 of 584 non-success rows (33 %) had spend measured in-process and dropped at the telemetry call — 11.8 M tokens.** Prism's ledger reconciles to the token and keeps recording after a client cancels (LULU: 99,210 tokens, 65 % after the cancel). Recommended C. | the 193-row half: 1db8b574 |
| [report_chain](audits/2026-09-06/report_chain.md) | The panel's only two endpoints read the **latest report system-wide**, never "report for cycle X"; with unenforced uniqueness (one cycle has 4 reports) the last duplicate wins. A crash orphans a `running` report forever and the client ORs it into `is_running`. The six-field write is all-or-nothing. | orphan: 9e31eb73; attribution: **open (trading-client)** |
| [command_queue](audits/2026-09-06/command_queue.md) | Five schedule-CRUD endpoints in trading-client write REFRESH_SCHEDULE with **no `status`/`created_at`** — the closed incident, on the new collection. `eval_worker` claims non-atomically. AUTORESEARCH duplicated for 4 cycles across months. Boot marks pending as error with no re-enqueue. | client fed58f55; claim/idempotency **open** |
| [boot_resilience](audits/2026-09-06/boot_resilience.md) | The loop-responsiveness test uses `time.sleep` and monkeypatches the real compute: **would not catch a revert**. Measured: the real transform via `to_thread` yields ~39 % of a free loop's ticks. **45/509 tickers have two vendors per date; the 30-period return spanned ~15 days.** `/health` is a literal that checks nothing. | dedupe: c8858ab4; test fidelity **open** |
| [tool_surface](audits/2026-09-06/tool_surface.md) | 5 force-added tools trip the off-whitelist canary on every call. `v3_portfolio_manager` has a 13-tool whitelist and has **never called a tool** (2,320 wasted prompt tokens/run). `get_finnhub_news` p95 **84 s**, max 148 s; `get_upcoming_events` has no cache (p50 22 s, 34 min/14 d); `get_institutional_holdings` does two unindexed scans per call. | canary: 9e31eb73; the rest **open** |
| [cycle_latency](audits/2026-09-06/cycle_latency.md) | **93.2 % model, 6.8 % tools**; agents strictly serial (max concurrency 1, idle 0). bear 460 s, quant 415 s, valuation 368 s mean. `cycle_benchmarks.collect_ms` is a confident zero with no producer. | nothing — informs the next pass |
| [signal_weights](audits/2026-09-06/signal_weights.md) | **Decorative.** Nothing multiplies `signal_weights` by anything; `signal_assessments` is 100 % prose; `action`/`internal_consensus_score` are declared by the model. `signal_weights_source` was stamped for the first time today (9/1,237 rows). | nothing — a product question |
| [memory](audits/2026-09-06/memory.md) | The read side is **dead by configuration**: `MEMORY_CONTEXT_ENABLED` defaults 0 and has never been set; 0 canonical memories reach any prompt. Write side: 1.36 runs/day, a 7-day outage 08-26→09-02 with no failure record, 51/51 over-threshold tickers stale. | nothing — an operator switch |
| [collectors](audits/2026-09-06/collectors.md) | `failure_cache` remembers only 404/410: **67.5 % of scrape failures are never cached**, one URL retried 1,545× in 2.5 days, 99.1 % of scrape-failure volume is waste. `"None"` is the logged reason 45,533×. `thenewsapi` yields nothing. The 4,900-char cut is blind. The 2,048-token embedder branch never fits any agent. | nothing |
| [frontend_test_plan](audits/2026-09-06/frontend_test_plan.md) | Vitest + jsdom + Testing Library, ~1 week. Real defect: `CycleControlBar.jsx:149-154` parses `checkpoint_ts` without the naive-UTC guard the other three sites have. `computeIsStale` is not an exported pure function. | **open** |
| [prism_side](audits/2026-09-06/prism_side.md) | Watchdog/queue/JSON-path confirmed with file:line; the shim's own constants match E1a-2 exactly. Two earlier claims corrected: `/agent/stop` exists (the gap is no reaper, and we never call it); the JANITOR collapse is **ours** (`prism_agent_registry.py:191-283`). | report only |

## Fixes shipped from these audits (batch 2, `fix/cold-fund-is-a-fund`)

- f0b7ea25 a fund with no metadata row is tiered from the vendor's `quoteType`
  (found by the observed cycle itself: JEPQ, `marketCap: null`, `totalAssets: $42.2B`).
- 1db8b574 three parse-failure telemetry sites pass the `prompt_tokens` in scope.
- 9e31eb73 boot marks an orphaned `running` report `interrupted`; four read-only
  force-adds join `_META_TOOLS` (`write_datastore` deliberately not).
- c8858ab4 `compute_sector_performance` keeps one vendor per ticker.
- trading-client fed58f55: REFRESH_SCHEDULE goes through a helper that writes
  the fields its poller filters on; a scanner guards every command write.

## Open, ranked by what it costs

1. **Outcome horizon** — every decision-quality number is on a drifted basis
   and is being fed back to the desks. Quarantine the 2,694 historical rows or
   bump `SCORE_VERSION`; fix `challenger.py:139`; store `entry_date`.
2. **Report attribution** — the panel must read the report for *this* cycle.
3. **Memory read side** — one flag; decide whether it should be on.
4. **Scraper failure cache** — remember thin-content/no-engine failures.
5. **Cancelled runs keep burning** — call `POST /agent/stop` on cancel, and
   sweep prism's ledger for the true cost (the 65 %).
6. **`get_upcoming_events` cache, `get_institutional_holdings` indexes,
   `v3_portfolio_manager`'s dead whitelist.**
7. The four audits that never ran.
