# Follow-up cycle improvements — 2026-09-08

The preceding audit is the baseline, not evidence that these changes help.
No target BUY/SELL outcome, confidence cap, or desired return is prescribed.

- [x] Correct native tool execution at the owned adapter boundary. Remove the
  reasoning acknowledgement from signed trading model catalogs; explicitly identify
  historical acknowledgements as unexecuted research. Preserve permissions, ordinary
  callers, real results, streaming progress and usage. Test JSON and SSE paths.
- [x] Check explicit numerical relationships in artifacts using deterministic math.
  Preserve the original prose, flag only unambiguous operand/formula errors, deliver
  corrections to downstream roles, and exclude known erroneous originals from learning.
  Supply current Board plan arithmetic to synthesis, distinguishing it from older plans.
- [x] Recheck trigger wording against monitor semantics and close any demonstrated
  contract gap. Do not infer that ticket assignment itself improves reasoning.
- [ ] Run frozen regressions and bounded model comparisons, retaining all attempts,
  failures, costs, delivery evidence and unchanged baseline results. Distinguish tool
  execution improvement from investment quality; absence of flagged arithmetic is not
  verified reasoning.
- [ ] Deploy affected owned services with live-cycle guards, verify running source
  and health, then validate a normal paper-enabled cycle and reconcile its outputs.
  Preserve other work and report any unavailable validation accurately.
- [ ] Publish outcomes and remaining limits. Learning benefit and profitability need
  larger held-out comparisons and properly matured paper outcomes.


Implementation validation: 7,055 unit tests passed, 97 skipped. The adapter passed
676 tests and TypeScript checking. Nineteen learning-lifecycle checks also passed
against an isolated Mongo database. Counts overlap and are not additive coverage.
A read-only scan of active canonical memories found no records excluded by the
final high-precision arithmetic rules. The original synthetic cohort has no matches
for these narrow patterns; most of its errors require broader semantic checks.
The saved MSFT artifacts contain seven flagged instances of three distinct errors:
zero-denominator multiple, cash decline, and gain off the SMA. The current Board
plan block separately supplies the widened-stop ratio. Raw records remain unchanged.

Source changes are frozen while the new model probe runs. Deployment and the next
normal paper-enabled cycle remain pending until the current BKE cycle finishes.
