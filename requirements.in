# ============================================================
# Production Dependencies (Docker image)
# ============================================================
# For testing/dev deps, see requirements-dev.txt

# Core: HTTP client, config, env
httpx==0.28.1
pydantic-settings==2.13.1
python-dotenv==1.2.2
paramiko==4.0.0

# Data Collection
yfinance==1.2.0
# psycopg was removed 2026-08-18 at Postgres teardown. Nothing under `app/` or
# `cycle_main.py` imports it any more — `app/db/connection.py` was the only
# importer and now lives at `scripts/migration/pg_connection.py`. The backfill
# and parity scripts that still read the frozen Postgres backup install it from
# `requirements-migration.in`; they are not in the image's import graph.
finnhub-python==2.4.27
edgartools==5.25.1
beautifulsoup4==4.14.3
fredapi==0.5.2

# Processors
ta==0.11.0
pandas==3.0.1
scikit-learn>=1.3.0
plotly==5.22.0

# Reddit/YouTube/News Collectors
ddgs>=9.14.0
markdownify>=0.13.0

# RLM (Recursive Language Models — MIT CSAIL)
# Pinned: monkey-patch in rlm_wrapper.py depends on rlm.core.rlm.get_client

# RAG / Embeddings
rank-bm25==0.2.2

# Web Server
fastapi==0.115.12
uvicorn[standard]==0.34.2
sse-starlette==2.4.1
python-multipart==0.0.20
orjson>=3.10.0

# Scheduling
APScheduler==3.11.2
pytz==2026.1.post1

# Math / Scientific Computing (Brain Graph)
numpy>=1.26.0

# Token Counting (accurate context window budgeting)
tiktoken>=0.7.0

# Runtime Toolbelt
tenacity>=8.5.0
structlog>=24.4.0
pandera>=0.20.4
concurrent-log-handler>=0.9.25

# MongoDB (Prism integration)
motor==3.4.0
pymongo>=4.0,<4.9  # motor 3.4.0 uses cursor._QUERY_OPTIONS, removed in 4.9

# Evaluation (judge_agent.py — runs in live pipeline, NOT dev-only)
deepeval>=2.0.0
rouge-score>=0.1.2

# HTTP compatibility
urllib3==1.26.18
chardet==5.2.0


# Neural TTS
piper-tts>=1.2.0
onnxruntime>=1.15.0
cloudscraper>=1.2.71

# ── Absorbed scraper-service (in-process scraping engines/collectors) ──
# Formerly the standalone scraper-service:8001. Engines: playwright/crawl4ai/
# vision; collectors: reddit/youtube/news/forums/etc. Chromium is installed in
# the Dockerfile (playwright install). NOTE: after editing this file, run
# `pip-compile --output-file=requirements.txt requirements.in` to relock.
playwright>=1.49
playwright-stealth>=2.0.0
crawl4ai>=0.8.5
trafilatura>=1.6.0
feedparser==6.0.12
yt-dlp==2026.3.17
youtube-transcript-api==1.2.4
twscrape>=0.12.0
